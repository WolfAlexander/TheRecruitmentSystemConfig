security:
  user:
    name: ${this.service.security.username}
    password: ${this.service.security.password}

server:
  port: 9090

eureka:
  server:
    evictionIntervalTimerInMs: 1000
  client:
    registerWithEureka: false
    fetchRegistry: false